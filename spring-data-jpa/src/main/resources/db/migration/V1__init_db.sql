CREATE TABLE author (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255),
   CONSTRAINT author_pkey PRIMARY KEY (id)
);

CREATE TABLE books (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   publication_year INTEGER,
   title VARCHAR(255),
   author_id BIGINT,
   CONSTRAINT books_pkey PRIMARY KEY (id)
);

ALTER TABLE books ADD CONSTRAINT fkdh6rjf063y1kmud3t0vrmrlo3 FOREIGN KEY (author_id) REFERENCES author (id) ON UPDATE NO ACTION ON DELETE NO ACTION;